[package]
name = "space-weather"
version = "0.1.0"
edition = "2021"

[features]
default = []
std = []
fetch = ["dep:reqwest", "std"]
fetch-blocking = ["fetch", "reqwest/blocking"]

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"], optional = true }
